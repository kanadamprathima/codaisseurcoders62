export const selectPosts = (reduxState) => reduxState.posts.posts;
export const selectDetails = (reduxState) => reduxState.posts.details;
export const selectComments = (reduxState) => reduxState.posts.comments;
